---
title: "Quickstart"
description: "Running your first task"
---

## Setting up

Get your free API key and start running tasks in minutes.

<Card
  title="Get your free API key"
  icon="key"
  href="https://zero.circlemind.co/developer"
  horizontal
>
  Unlock your free welcome credits. No credit card required.
</Card>

## Install Smooth
Skip if not using the Python SDK

<CodeGroup>

```bash Python
pip install smooth-py
```

</CodeGroup>

## Run a task
Submit a task in seconds

<CodeGroup>

```python Python
from smooth import SmoothClient

smooth_client = SmoothClient(api_key="cm-YOUR_API_KEY")
task = smooth_client.run("Go to google flights and find the cheapest flight from London to Paris today")

print(f"Live URL: {task.live_url()}")
print(f"Agent response: {task.result()}")
```

```javascript Javascript
fetch('https://api2.circlemind.co/api/v1/task/smooth', {
  method: 'POST',
  headers: {
    'apikey': 'YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({ 'max_steps': 32, 'task': 'your_task_description' })
})
```

```curl cURL
curl -X POST https://api2.circlemind.co/api/v1/task/smooth \ 
          -H "apikey: cm-YOUR_API_KEY" \
          -H "Content-Type: application/json" \
          -d '{"max_steps": 32, "task": "your_task_description"}'
```

</CodeGroup>

## Next steps
That's all it takes to start automating with Smooth!

If you want to learn how to implement more complex workflows in Python, check out our [examples](/examples/).

Or, dive deep into our API and read about the different parameters on our [API Reference](/api-reference/introduction).

<Note>
  To play around with the API, check out our [playground](https://zero.circlemind.co/developer).
</Note>
